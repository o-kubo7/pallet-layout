# 退避スペースとなぞり選択 — 作業記録

- 日付: 2026-09-03
- 対象: `files/index.html` / `files/sw.js`
- 設計書: `docs/superpowers/specs/2026-09-03-stash-space-and-sweep-select-design.md`

---

## 1. 状態

| | |
|---|---|
| ブランチ | `claude/intuitive-layout-editing-4nszpb` |
| PR #1 | **マージ済み**（2026-09-03 20:12 UTC / マージコミット `ae7a699`） |
| GitHub Pages | 実行 #9 成功。公開版に反映済み |
| CACHE_VERSION | v27 → v28 → **v29**（マージ後の修正で再度上げた） |

マージ後、実機で使って見つかった修正を別途入れている（§10）。

```
ae7a699 Merge pull request #1        ← main に入った
c0975f7 docs: 作業記録を残し、README を実態に合わせる
36a672f chore: CACHE_VERSION を v28 に上げる
6e53c36 feat: 配置編集に退避スペースとなぞり選択を足す
78e88a4 docs: 退避スペースとなぞり選択の設計書を追加する
a431c47 Merge feat/sheet-top-overflow-rescue（マージ前の main）
```

---

## 2. 何を足したか

配置編集タブに2つ。どちらも「荷物が多い日に編集が進まない」を外すためのもの。

### 退避スペース

倉庫に実在しない、画面の中だけの置き場。`DEFAULT_SPACES` に `zone:"stash"` /
`sheet:"none"` のエリアを1つ足しただけで、実体は他のエリアと同じ「列の集まり」。
`moveCells` / `validateMove` はエリア名と列番号で動くので、**移動処理は変えずに**
出し入れが成立した。位置は右上／左上／下から選べる（右利き・左利き）。

### なぞり選択

指でなぞって同じロットのマスをまとめて選ぶ／外す。選択済みのマスから
選択の外へ出すと運ぶ。PC とスマホで判定は同じで、違うのは
「指のときだけ画面を動かす／なぞるの2択が出る」ところだけ。

---

## 3. 進め方（この順で決めた）

1. **本体のコードを読む調査から入った。** 当初 `index.html`（681行・リポジトリ直下）を
   見ていたが、実際の本体は `files/index.html`（3402行）で、`main` が大きく進んでいた。
   ここに気づくまで前提がずれていた
2. **モックで操作感を先に決めた。** `files/index.html` そのものに置換を当てた
   モックをアーティファクトとして公開し、実機（iPhone）で触って直す、を繰り返した
3. **決まってから本体へ。** モック専用の細工を外した同じ置換を `files/index.html` に当てた
4. 設計書を書き、コミットを3本に分け、PR を作成

**モックを先に作ったのが効いた。** 指の操作は書いて決められず、実機で触って
初めて分かることが多かった（§4）。

---

## 4. 実機で触って変えたこと

書面では正しく見えたが、触ると駄目だったもの。

| 当初 | 実機で分かったこと | 直した内容 |
|---|---|---|
| 退避スペースを盤の下に置く | 指の移動が大きすぎる | 隅（既定は右上）に浮かせ、位置を選べるようにした |
| 選ぶ／外すをボタンで切り替える | **押さないと分からない機能は無いのと同じ**（Alt キーの案も廃止） | 指の動きだけで自動判定にした |
| 運び始めの合図は落とし先の色分け | 他のマスに重ねるまで何も起きず、移動中だと分からない | 元のマスから離れた時点で4つ同時に出す |
| 掴んだら受け皿に「⇩ ここへ」と出す | 素の状態から「ここへドラッグ」と書いてある。二重 | 枠の強調だけにした |

---

## 5. 検証

`file:///.../files/index.html` を直接開き、ヘッドレスブラウザ（Playwright）で実行。
`file://` では Service Worker が登録されないので、キャッシュの回り道は起きない。

- 選ぶ・外す・運ぶの5パターン／なぞりの取り消し
- 運び始めの表示（列の隙間では出さず、1マス分離れたら出る。余白の上でも出たまま）
- 退避スペース（受け皿・自動配置し直しでも残る・中のマスから戻す）
- 位置の切り替えと案内の表示切替（案内オフでも取り消しは出る）
- 既存機能（配置表が描ける／紙に「退避」が出ない／スペース設定に出ない・消えない／リロードで復元）

**配置表が変わっていないことの実測**: `plans/2026-09-01-sheet-top-overflow-rescue.md` の
`window.probe` とフィクスチャ（FN / FR / FP）をそのまま使い、`a431c47` と本ブランチで
幾何と紙の中身を突き合わせ、3つの日すべて一致を確認した。

```
FN: colCount 14 / colW ["48px"] / tableW 673.4 / tableH 473.2
    上段欄 [96,96,96,96] / 下段欄 [96×7] / 引出線 5 / top 3 / bottom 6
```

配置表まわりの12関数（`sheetPlacement` `sheetLayout` `sheetSlots` `sheetAreas`
`renderSheet` `mergeEntries` `drawLeaders` `gridRows` `gridNoticeRow`
`fitSheetText` `printSheet` `applyDisplay`）はハッシュ一致で無変更。

**実機（iPhone）でも確認済み**: なぞりの効き、スクロールが従来どおりできること、
運び始めの表示。

---

## 6. 途中で見つけて直した不具合

- **「設定を反映」で退避スペースが消えた。** 設定テキストから除いたので、
  一覧を組み直すときにエリアごと落ちていた。`withStash()` で足し直すようにした。
  モックでは設定タブを触っておらず、本体に当ててから出た
- **自分で外したマスを「選択の外」と誤判定していた。** 1マス外した次の瞬間に
  運びへ切り替わっていた
- **運びへ切り替えるときにポインタの捕捉を解いていた。** `lostpointercapture` が飛び、
  直後に組み立てた移動処理を既存のハンドラが片付けていた
- **退避スペースがタブ列を覆っていた。** `.tabs` は `position:sticky` なので、
  タブの高さを実測してその下に貼りつけるようにした

---

## 7. 本体に入れていないもの（モック専用）

アーティファクトは iframe の中で動くため、モックにだけ次の細工が入っている。
`build.py`（この会話の作業用スクリプト。リポジトリには入れていない）の
`PROD=1` で除外している。

- 試作タイトル・試作バッジ
- `confirm` / `alert` を画面上部の帯に差し替える細工
- Service Worker の登録を止める細工

---

## 8. 注意

- **`SPACES_SAVE_VERSION` を 2 → 3 に上げた。** `DEFAULT_SPACES` が変わったため。
  既存端末の保存済みスペース設定と手動調整は初期値に戻り、設定タブにその旨が出る
  （既存の仕組みのまま）。**搬入日の直前のマージは避けるのが安全**
- リポジトリの**既定ブランチが `main` ではなく `StreamHelperNano-app`** になっている
  （`git ls-remote` の HEAD）。GitHub 上で PR を作ると base が誤って選ばれる。
  Settings → Branches で `main` に変えておくとよい

---

## 9. 積み残し

今回の範囲外。必要になったら着手する。

- **列を指定するピン留め**。退避から戻すとき、元の位置には戻らない
- **ひとなぞりを超える取り消し**。Undo は「ひとなぞり」の単位のみ
- **`manifest.json` のアイコンのパスが合っていない**（今回の変更とは無関係の既存の状態）。
  ワークフローは `files/` を公開ルートに配るので、実体は `/icon-192.png` になるが、
  `manifest.json` は `./icons/icon-192.png` を指している。
  `index.html` の `apple-touch-icon` は `./icon-192.png` で正しい。
  ホーム画面追加のアイコンが出ていない可能性がある
- **`docs/memos/` がリポジトリに入っていない**。5つの設計書が
  `docs/memos/2026-08-27-sheet-improvements-memo.md` を参照しているが、
  コミットされていない（`.gitignore` にも無いので、単に未追加）。
  このファイルが `docs/memos/` の最初のコミットになる

---

## 10. マージ後の修正

### 退避スペースから倉庫へ戻すと、意味のない確認が出た（2026-09-03 実機）

> 部品1 / 5555 が **0か所から 1か所に分かれます**。このまま移動しますか？

`blockCountOf` は退避スペースを数えない（設計書 §7 エッジケース）。そのためロットを丸ごと逃がすと
倉庫側の数が 0 になり、戻すときに 0 → 1 で `after > before` が成り立って
`needConfirm` が立っていた。**0 → 1 は分割ではなく最初の設置**なので、
`validateMove` の判定に `before > 0` を足した。

```js
// 直す前
return {ok:true, needConfirm: after>before, before, after, next};
// 直したあと
return {ok:true, needConfirm: before>0 && after>before, before, after, next};
```

倉庫に1マスでもあるロット（`before >= 1`）の挙動は変わらない。
実際に「1か所 → 2か所」では今までどおり確認が出ることを試験で確かめてある。

**モックでは出なかった。** 退避へ逃がしたあと倉庫へ戻す往復を、
ロットを丸ごと逃がした状態で試していなかったため。部分的に逃がすと
倉庫側に残りがあるので `before >= 1` になり、この経路を踏まない。
